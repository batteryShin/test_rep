#include <stdio.h>
#include <stdlib.h>
#include <iostream>
#include <vector>

using namespace std;

int T;
int M;
int C;
int coins[100];

int solve(int money) {
    // base condition
    if( 
}

int main() {
    cin >> T;
    while(T-->0) {
        cin >> M >> C;

        vcoin.clear();
        for(int i=0; i<c; i++) {
            cin >> coins[i];
        }

        cout << solve(coin,M) << "\n";
    }
}
